

h1 Mixin Example


mixin bootstrap-button(label, theme='default')

    a.btn(class='btn-'.$theme)= $label


mixin bootstrap-carousel

    .carousel
        // Code for bootstrap carousel


//Attribute forwarding
mixin alias
    p.class-1&classes($class)


p Watch mah buttons!
+bootstrap-button('Send')
+bootstrap-button('Reply', 'info')

p Notice how the bootstrap-carousel mixin isn't rendered, because we didnt use it!


+alias.class-2.class-3&classes('class-4', 'class-5')
